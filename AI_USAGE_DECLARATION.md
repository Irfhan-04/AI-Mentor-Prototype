# AI Usage Declaration

*Draft — finalize after live validation (Phase 2) and report writing
(Phase 3). This is a living document; update it as work progresses rather
than writing it retroactively at the end.*

## Tools used

- **Claude (Anthropic)** — architecture decision-making (approach and
  provider selection), prompt design (`src/prompts.py`), schema design
  (`src/schema.py`), core implementation (`src/mentor.py`,
  `src/config.py`, `src/cli.py`, `src/logging_config.py`), all mocked unit
  tests, and this documentation set.
- **[Codex or your own hands — fill in once Phase 1/2 is run]** — live API
  integration testing, notebook execution, git operations.

## What was and wasn't AI-generated

Generated by Claude, reviewed and corrected by a human before being
treated as final:

- All files under `src/` and `tests/`
- `ASSESSMENT_BRIEF.md`, `DECISIONS.md`, `BUILD_SPEC.md` (this planning
  set)

One correction is on record deliberately, as an example of the review
process rather than something to hide: an early draft claimed Gemini's
`response_schema` doesn't reliably enforce `minItems`/`maxItems` on
arrays. That was wrong — checked against current docs and fixed, with a
test added (`test_raw_schema_bounds_match_constants`) proving the fix
holds. Kept in `DECISIONS.md` as the record of the correction.

Not yet generated, will be added once real:

- Live-run outputs for the 4 sample ideas (Phase 2)
- Final Architecture Decision Report narrative (Phase 3) — will be written
  from `DECISIONS.md` plus real Phase 2 output, not from assumptions about
  what the output would look like
- Final Failure Analysis section — will cite the actual adversarial-run
  output from Phase 2, with the mocked-test result in
  `tests/test_prompt_injection.py` as supporting evidence, not as a
  substitute for a real run

## What was explicitly rejected from AI drafting

None yet — nothing has been discarded outright. If something is rewritten
from scratch rather than corrected, note it here with why.
